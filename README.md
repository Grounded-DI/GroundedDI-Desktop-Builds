# GroundedDI-Desktop-Builds
Development repository for Grounded DI native desktop applications. Documents local-first software architecture, source code, build workflows, packaging, testing, and platform-specific implementation for macOS and Windows.

BriefWise DI² is a local-first legal drafting and validation system that uses a deterministic Rust kernel to control when candidate legal text may become committed, replayable, and exportable. It implements 20 formulas, 14 runtime stages, 15 FilingReady gates, canonical serialization, SHA-256 binding, audit and replay controls, and fail-closed export authorization. The current v0.1.0 release runs locally with network providers disabled and includes a validated synthetic filing-integrity workflow.

Topics:
legal-tech rust tauri deterministic-systems audit-trail replay legal-ai local-first sha256 typescript
